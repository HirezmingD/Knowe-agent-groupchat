# 安全策略

**English version: [SECURITY.md](./SECURITY.md)**

## 报告漏洞

如果您在 Knowe 中发现安全漏洞，请**私下报告**——不要公开发布 issue。

**报告方式：**
- 邮箱：**jhezhou@163.com**（或 jhezhou@gmail.com）
- 报告内容请包含：受影响版本、漏洞描述、复现步骤，以及（如有可能）概念验证代码。

我们承诺在 **3 个工作日内**确认收到报告，并持续向您通报修复进展。

## 安全设计说明

- **本地优先**：所有数据（项目、聊天记录、知识库、Token 账本）都保存在用户自己的机器上。无云存储、无遥测。
- **执行沙箱**：在受支持的 Windows 11 主机上，模型生成的 Shell 与 Python 进程通过 Microsoft Execution Containers 运行，项目工作区是唯一可写根目录，默认禁止对外网络。原生隔离无法被证明时，终端会关闭并返回错误，绝不回退到宿主用户权限。文件工具仍是经过路径校验的窄权限代理；只有用户明确放行的文件（如已签名附件）可从工作区外读取。
- **预览边界**：固定的 MXC 0.7 确实使用 Windows AppContainer 与 Job Object，但微软仍将 MXC 标记为早期预览，并明确提醒当前 profile 不能被当作完整安全边界。Knowe 额外固定策略、检查链接/ACL、套一层独立资源 Job 并做越界攻击测试；它是操作系统强制的进程隔离，不是虚拟机，也不能替代在一次性机器里运行真正敌对的代码。
- **本地端口**：后端仅监听 `127.0.0.1`（WebSocket 8080、健康检查 8081）——仅回环，不暴露到网络。
- **API 密钥**：界面中保存的 Key 使用当前 Windows 用户范围的 DPAPI 静态加密，并对整份设置投影做真实性校验；不写入浏览器存储、不传给 Agent 终端进程，也不会提交到代码仓库。DPAPI 保护的是离线存储，同一 Windows 用户身份下已经运行的恶意程序不在此保证内。
- **网页隔离**：保留 Chromium 原生沙箱。Agent 控制的网页与浏览器流量只允许公网 HTTP(S)，每次重定向和子资源都会重新校验；回环、局域网、链路本地与保留地址一律拒绝。
- **开发工具与应用完整性**：正式安装包关闭 Chrome/Node 远程调试，并在 Electron 二进制中熔断 `RunAsNode`、`NODE_OPTIONS` 与 Node Inspector；应用只从完整性校验过的 ASAR 加载。受信任的渲染器入口目前仍通过 `BrowserWindow.loadFile()` 加载，因此将 `GrantFileProtocolExtraPrivileges` 保持启用，以支持其模块与子资源；不可信项目 HTML 只会进入禁用脚本的沙箱 `srcdoc`，旧 `/preview/tree` HTTP 服务面已移除。开发版调试也必须显式开启且只绑定回环地址。
- **更新边界**：本地加固构建默认不连接或安装第三方上游自动更新，防止未经同等安全门禁的版本覆盖加固。恢复自动更新前必须迁移到受控、签名的发布通道。

## 适用范围

本策略覆盖 Knowe 应用及其源代码。第三方依赖中的漏洞请上报给对应的上游项目。
