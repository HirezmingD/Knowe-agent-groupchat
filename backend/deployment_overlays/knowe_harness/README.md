# Harness overlay (v2.2)

The Worker no longer requires a separate work-order authority type. Deploying this
overlay makes older harness layouts import the shared `backend.runtime.TaskEnvelope`
directly.
