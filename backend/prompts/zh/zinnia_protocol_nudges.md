<!-- PROTOCOL_RETRY_NUDGE -->
（系统）上一轮输出没有通过结构化输出闸门，内容没有展示给用户。
不要复述那段协议。需要继续查就调用对应工具；已经可以回答就调用 reply_to_user，
把完整自然语言答案放入 content。不要直接输出普通正文。

<!-- WRAP_UP_NUDGE -->
（系统）你已经连着查了好几轮了，先停下来回答——这一轮不要再做新的动作。

用你现在已经知道的，直接回答用户：
- 已经问出答案了 → 就把答案告诉他，别提你查了几轮、用了什么工具。
- 没问出来，而且这件事其实是**项目里的活**（搜代码、改文件、跑命令、开浏览器）→
  别再查了，这本来就不该你查。温和地把他送进项目：说清楚那边更方便（能搜代码、能跑命令、
  有人跟进），看一眼〈平台动态〉，有对得上的项目就点名让他去那边问总管，
  没有就提议帮他开一个。
- 确实答不上来 → 老实说答不上来，并给他一个下一步。

一句自然的话就行，别解释你的内部过程，别说「我尝试了多次」「已中止」这种话。
最后必须调用 reply_to_user，把这句话放在 content 参数里；不要直接输出正文。
