# Direct runtime dependencies are exact so a clean security build cannot
# silently resolve to a different execution or network stack.
websockets==17.0.1
httpx==0.28.1
python-dotenv==1.2.2
playwright==1.62.0
ddgs==9.14.4
PyYAML==6.0.3
